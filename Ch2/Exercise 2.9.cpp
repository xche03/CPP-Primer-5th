#include <iostream>
using namespace std;
int main (){

	//std::cin >> int input_value;//error type name is not allowed.
	//int i = { 3.14 };//list initialization Form of initialization that uses curly braces to enclose one or more initializers.
	/*When used with variables of built-in type, this form of initialization has one
important property: The compiler will not let us list initialize variables of built-in type if
the initializer might lead to the loss of information:*/
	//double salary = wage = 9999.99;//identifier "wage" is undefined
	int i1 = 3.14;
	return 0;


}